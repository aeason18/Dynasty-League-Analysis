// Hex mirrors of the "Broadcast" theme's --primary (sage) and --pop (magenta)
// tokens in app/globals.css. Three.js materials can't read CSS custom
// properties directly, so these are kept in sync by hand with the palette
// those oklch tokens were derived from.
export const CRYSTAL_ACCENT_HEX = 0x9cc98a;
export const CRYSTAL_POP_HEX = 0xc662ab;
